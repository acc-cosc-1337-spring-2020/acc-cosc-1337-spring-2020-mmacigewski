/*//Create ref function w reference and pointer parameter 
void ref_pointers(int& int1, int* int2); //passes int1 by reference, passes int2 by reference
{
	int1 = 100;
	*int2 = 69; //the * dereferences it allowing it to change value, without the * it passes the address
}
//Create return pointer8 function*/